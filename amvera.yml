meta:
  environment: jvm
  toolchain:
    name: gradle
    version: 11
build:
  artifacts:
    build/libs/*.jar: /
run:
  jarName: devmark-ru-bot-1.0.0.jar
  persistenceMount: /data
  containerPort: 8080
